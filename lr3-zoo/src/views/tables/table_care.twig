<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Зоопарк у дома</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    {% include 'header.twig' %}
    <div class="table-container">
        <h2>Уход за животными</h2>

        <form method="GET" action="">
        <div class="form-group">
            <label for="filter_name">Фильтр по названию:</label>
            <input type="text" name="filter_name" id="filter_name" value="{{ selected_name }}">
        </div>
            <button type="submit">Применить</button>
        </form>
        <br>
        <br>
        <div class="table-con">
            <table>
                <tr>
                    <!-- <th>care_id</th> -->
                    <th>Название животного</th>
                    <th>Уход</th>
                    <th></th>
                </tr>
                {% for care in cares %}
                    <tr>
                        <!-- <td>{{ care.care_id }}</td> -->
                        <td>{{ care.animal_name }}</td>
                        <td>{{ care.care_type}}</td>
                        <td>
                            <button id="button_{{ care.care_id }}">Редактировать</button>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="3">Нет данных</td>
                    </tr>
                {% endfor %}
            </table>
        </div>
        <div class="form-group">
            <a href="/cares/pdf?filter_name={{ selected_name }}" target="_blank">Скачать PDF</a>
        </div>
    </div>
</body>
</html>